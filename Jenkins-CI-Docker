pipeline {
  environment {
    dockerimagename = "siddarth8696/my_repo1:tomee"
    registry_url = "https://registry.hub.docker.com"
    registryCredential = 'sid'
    }
    tools {
        maven 'maven3.8.6'
    }
    
	agent {
     label 'CI-Jenkins-Slave'
  }
  
  stages {
    stage('Cloning Git') {
      steps {
        checkout scm
      }
	}
	
	 stage ('Build Package') {
      steps {
          sh 'mvn clean package'
         }
       }
    stage('Building image') {
      steps{
        script {
         
         dockerImage = docker.build siddarth8696/my_repo1:latest
        }
      }
    }
    stage('Pushing Image') {
      steps{
        script {
          docker.withRegistry( https://registry.hub.docker.com,sid ) {
            dockerImage.push("${env.BUILD_NUMBER}")
            dockerImage.push("siddarth8696/my_repo1")
          }
        }
      }
    } 
	
  }
} 
